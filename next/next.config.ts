import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
      images: {
        domains: ['cdn.sanity.io', "monikahelgesen.com"], // Add the hostname here
      },
};

export default nextConfig;
